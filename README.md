# minigolf
A simple 2-D minigolf game using OpenGL in C++.  
After cloning, run `g++ *.cpp -lGL -lGLU -lglut -o main && ./main` to start the game.  
To play the game, use mouse to click and drag the ball.
